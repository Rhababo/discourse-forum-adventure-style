{{#if isAdventureTopic}}
  <script type="text/discourse-plugin" version="2.5">
    api.attachWidgetAction('post','nextUpdatePost',() => {
      var nextUpdatePostUrl = this.getURL();
      console.log(nextUpdatePostUrl);
    });
    api.addPostMenuButton("next-update", () => {
      return{
        action: 'nextUpdatePost',
        icon: 'caret-right',
        className: 'next-update',
        title: 'next.update',
        position: 'last'
      };
    });
  </script>
  <script>
    console.log("Adventure Topic is loaded.");
  </script>
  Adventure Topic is loaded.
{{/if}}
{{log @outletArgs}}